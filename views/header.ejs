<nav class="bg-orange">
  <div class="px-4 mx-auto max-w-7xl sm:px-6 lg:px-8">
    <div class="flex items-center justify-between h-16">
      <div class="flex">
        <div class="hidden sm:ml-6 sm:flex sm:space-x-8 items-center">
          <h1 style="font-weight: bolder; font-size: larger">WORLD CUP</h1>
          <a
            href="/"
            class="inline-flex items-center px-1 pt-1 text-sm font-medium text-white border-b-2 border-transparent hover:border-gray-300 hover:text-gray-700"
            >Home</a
          >
          <a
            href="/nations"
            class="inline-flex items-center px-1 pt-1 text-sm font-medium text-white border-b-2 border-transparent hover:border-gray-300 hover:text-gray-700"
            >Nations</a
          >
          <!-- ------------------------- -->
          <% if (isAuth == true && isAdmin == true) { %>
          <!-- -------- -->
          <a
            href="/user/accounts"
            class="inline-flex items-center px-1 pt-1 text-sm font-medium text-white border-b-2 border-transparent hover:border-gray-300 hover:text-gray-700"
            >Accounts</a
          >
          <% } %>
        </div>
      </div>
      <div class="flex gap-3">
        <!-- ------------------------- -->
        <% if (isAuth == true) { %>
        <!-- -------- -->
        <div class="flex gap-4">
          <div>hello, <%= user.username%></div>
          <a
            href="/user/me"
            class="inline-flex items-center rounded border border-transparent bg-orange px-2.5 py-1.5 text-xs font-medium text-white shadow-sm hover:bg-love focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
            >My profile</a
          >
          <a
            href="/user/logout"
            class="inline-flex items-center rounded border border-transparent bg-white px-2.5 py-1.5 text-xs font-medium text-love shadow-sm hover:text-love focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
          >
            logout
          </a>
        </div>
        <% } %>
        <!-- ------------------ -->
        <% if (isAuth == false) { %>
        <a
          href="/user/login"
          class="inline-flex items-center rounded border border-transparent px-2.5 py-1.5 text-xs font-medium text-white shadow-sm hover:bg-orange focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
        >
          Login
        </a>
        <a
          href="/user/register"
          class="inline-flex items-center rounded border border-transparent bg-white px-2.5 py-1.5 text-xs font-medium text-love shadow-sm hover:text-love focus:outline-none focus:ring-2 focus:ring-indigo-500 focus:ring-offset-2"
        >
          Register
        </a>
        <% } %>
      </div>
    </div>
  </div>
</nav>
